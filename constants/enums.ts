/**
 * you can choose to keep all
 * enums for your app in here or separate them
 * however you choose to.
 */

export enum Cookies {
  ACCESS_TOKEN_KEY = 'trstack-access-token', // change to preffered name
  REFRESH_TOKEN_KEY = 'trstack-refresh-token', // change to preffered name
  MAX_AGE = 30 * 24 * 60 * 60, // 30 days (1 month)
}
